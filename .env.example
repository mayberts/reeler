# Drizzle
DATABASE_URL=local.db

# Plex
PLEX_SERVER_URL=http://your-plex-server:32400
PLEX_TOKEN=
# Shared secret used in the webhook URL path (/api/webhooks/plex/<token>), since
# Plex does not sign webhook requests. Generate any random string.
PLEX_WEBHOOK_TOKEN=
